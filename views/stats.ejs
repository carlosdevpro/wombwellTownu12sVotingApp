<%- include('partials/header') %>

<div class="container mt-4">
  <!-- Back to Home button -->
  <div class="mb-4 d-flex justify-content-start">
    <a href="/" class="btn btn-neon-sm btn-block">🏠 Back to Home</a>
  </div>

  <h2 class="text-center mb-4">📊 Player Stats</h2>

  <div class="row gy-4 mb-5">
    <!-- 👈 Added gy-4 for vertical spacing -->
    <div class="col-md-4">
      <h4 class="text-center">⚽ Goals</h4>
      <ul class="list-group">
        <% goals.forEach(p => { %>
        <li
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <%= p.firstName %> <%= p.lastName %>
          <span class="badge bg-primary rounded-pill"><%= p.goals %></span>
        </li>
        <% }) %>
      </ul>
    </div>
    <div class="col-md-4">
      <h4 class="text-center">🎯 Assists</h4>
      <ul class="list-group">
        <% assists.forEach(p => { %>
        <li
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <%= p.firstName %> <%= p.lastName %>
          <span class="badge bg-success rounded-pill"><%= p.assists %></span>
        </li>
        <% }) %>
      </ul>
    </div>
    <div class="col-md-4">
      <h4 class="text-center">🏅 MOTM Wins</h4>
      <ul class="list-group">
        <% motmWins.forEach(p => { %>
        <li
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <%= p.firstName %> <%= p.lastName %>
          <span class="badge bg-warning rounded-pill"><%= p.motmWins %></span>
        </li>
        <% }) %>
      </ul>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
